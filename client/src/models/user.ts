export interface IUser {
    email: string;
    isActivated: boolean;
    name: string;
    id: string
}
export interface IUserInfo {
    id: string,
    name: string,
    email: string,
    isActivated: boolean,

}
